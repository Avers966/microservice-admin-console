spring:
  application:
    name: microservice-admin-console

  cloud:
    kubernetes:
      secrets:
        sources:
          - name: microservice-admin-console
      config:
        sources:
          - name: library-core
          - name: microservice-admin-console
      reload:
        enabled: true

management:
  endpoint:
    restart:
      enabled: true
    health:
      enabled: true
    info:
      enabled: true
    prometheus:
      enabled: true
